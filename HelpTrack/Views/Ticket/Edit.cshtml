@model HelpTrack.Application.DTOs.TicketDTO

@{
    ViewData["Title"] = "Editar Ticket";
}


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdTicket" class="control-label"></label>
                <input asp-for="IdTicket" class="form-control" readonly/>
                <span asp-validation-for="IdTicket" class="text-danger" ></span>
            </div>
            <div class="form-group">
                <label asp-for="Titulo" class="control-label"></label>
                <input asp-for="Titulo" class="form-control" />
                <span asp-validation-for="Titulo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Descripcion" class="control-label"></label>
                <input asp-for="Descripcion" class="form-control" />
                <span asp-validation-for="Descripcion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPrioridad" class="control-label"></label>
                <input asp-for="IdPrioridad" class="form-control" />
                <span asp-validation-for="IdPrioridad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdEstadoActual" class="control-label"></label>
                <input asp-for="IdEstadoActual" class="form-control" />
                <span asp-validation-for="IdEstadoActual" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdUsuarioCreacion" class="control-label"></label>
                <input asp-for="IdUsuarioCreacion" class="form-control" readonly/>
                <span asp-validation-for="IdUsuarioCreacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Imágenes del Ticket</label>

                <div class="d-flex flex-wrap gap-2">
                    @if (Model.ImagenesTicket != null && Model.ImagenesTicket.Any())
                    {
                        @foreach (var img in Model.ImagenesTicket)
                        {
                            <div class="position-relative me-2 mb-2">
                                <img src="@Url.Content(img.UrlArchivo)" alt="Imagen del ticket" class="img-thumbnail" style="width:150px; height:auto;" />
                            </div>
                        }
                    }
                    else
                    {
                        <p class="text-muted">Este ticket no tiene imágenes adjuntas.</p>
                    }
                </div>
            </div>
            <div class="form-group mt-3">
                <label for="newImages">Agregar nuevas imágenes</label>
                <input type="file" id="newImages" name="NuevasImagenes" multiple class="form-control" accept="image/*" />
            </div>
            <div class="form-group mt-4">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Guardar cambios
                </button>
                <a asp-action="Details" asp-route-id="@Model.IdCategoria" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Cancelar
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
